[phases.setup]
nixPkgs = ['python311', 'ffmpeg', 'nodejs_20', 'deno']

[phases.install]
cmds = [
    'python -m venv /opt/venv',
    '/opt/venv/bin/pip install --upgrade pip',
    '/opt/venv/bin/pip install -r requirements.txt'
]

[phases.build]
cmds = []

[start]
cmd = 'bash start.sh'
